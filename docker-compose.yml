services:
  app:
    build:
      context: .
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    command: yarn dev --host 0.0.0.0 --port 5173

volumes:
  node_modules:
